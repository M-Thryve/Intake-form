# 06 — UI Surface & Factory Console (PLAN)

Retroactively synthesized from `PHASE_5_COMPLETION_REPORT.md` and `PHASE_6_COMPLETION_REPORT.md` to satisfy the GSD UI-review input gate. Canonical record: those repo-root files.

## Intended scope

Phase 5 (frontend) delivered the Factory Console and consolidated frontend integration:

- `src/console/ReviewQueue.tsx` — review queue with filtering, sorting, auto-refresh
- `src/console/IntakeDetail.tsx` — 8-section collapsible intake detail (A–H)
- `src/console/OwnerDecision.tsx` — decision panel + confirmation modal, reason validation, 409 handling
- `src/console/McpStatusPanel.tsx` — MCP run status icons + bounded retry (3)
- `src/console/BuildCardView.tsx` — read-only Build Card with preliminary labeling
- `src/console/AuditTrail.tsx` — chronological audit viewer with "Load more" pagination
- `src/console/ConsoleApp.tsx` — refactor to delegate to ReviewQueue + IntakeDetail
- `src/api/console.ts` — console API client, 403/409 handling
- `src/__tests__/console-integration.test.tsx` — 10 integration tests
- `server/src/migrations/009_client_id_backfill.sql`

Client-facing intake wizard (`src/App.tsx`) carried inline validation (REV-01), reduced deck options (REV-04), project-type gating (REV-02), design-step removal (REV-05), and "Continue" button label (REV-06).

## Verification intent

- TypeScript clean (`tsc --noEmit`), Vite build success, all vitest suites pass, console queue + decision + MCP + Build Card + audit behaviors verified via mock integration tests.
- Phase 6 verification phase executed workflow/security/audit/migration checks end-to-end; defects cataloged but not fixed.

## Notes for auditor

- No UI-SPEC.md exists → audit against abstract 6-pillar standards.
- Both the intake wizard (`src/App.tsx`) and the Factory Console (`src/console/*`) are in scope.