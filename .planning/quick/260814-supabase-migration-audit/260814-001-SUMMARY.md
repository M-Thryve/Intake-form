# Quick Task 260814-001: Supabase Migration Audit + Apply — Summary

**Status:** done (16/16 migrations applied to `ilbyzsktnllevfbomesc`)

## What was done

- **Audited live Supabase project `ilbyzsktnllevfbomesc`** via PostgREST (service key):
  - 39/40 expected tables missing (404 `PGRST205`); only legacy `idempotency_keys` exists.
  - Project reachable + key valid (`auth/v1/health` 200).
  - Conclusion: **no migrations applied** (matches DEPLOYMENT.md + phase reports' open items).
- **Prepared apply artifacts** (in this directory):
  - `migrations-bundle.sql` — 16 migrations, ordered + section-marked (2652 lines).
  - `preflight-idempotency-keys.sql` — renames conflicting legacy `idempotency_keys` table.
- **Applied all 16 migrations** via Management API `database/query` (`apply-migrations.mjs`):
  - Preflight rename OK → `000`–`015` all OK (**16/16**).
  - 48 public tables live; all 40 expected M-THRYVE tables present (verified `verify-final.mjs`).
  - Smoke test OK: service key in `server/.env` queries `intakes` and `vouchers` (200 `[]`).

## Migration-file fixes (were blocking 005/006/009/012/013/014)

| File | Bug | Fix |
|------|-----|-----|
| `005_agreement_finance.sql` | referenced `verification_status` (never created); server needs voucher columns | added `voucher_code/owner_client_id/discount_percent/expires_at/updated_at` on `vouchers` (+ rename `code`→`voucher_code`) and `submitted_code/verification_status/discount_amount_php/verified_at/verified_by` on `intake_voucher_redemptions` |
| `009_client_id_backfill.sql` | read flat `i.full_name/company/email/phone` (not in schema) | read from `i.client_details->>'...'` |
| `012_outbox_automation.sql` | view joined `a.action` (column is `event_type`) | `a.event_type` |
| `013_client_identity.sql` | `ON CONFLICT (normalized_email)` can't match partial unique index | made `idx_clients_normalized_email` non-partial |

## Wrong-project detour (documented, NOT on target)

- The CLI's own account project `ckektudtegnodczibkgx` is a DIFFERENT booking app (leads/bookings with live
  data); **9/16 migrations were wrongly applied there** before the real project was identified. Rollback
  pending user decision (rollback files exist in `server/src/migrations/rollback/`).
- `lusziumbodejmtdzuzbf` (BUILD IT WORKSPACE) hosts the full M-THRYVE schema with live data and is NOT the
  `.env` target; 5 missing tables were created there as a no-op safeguard.

## Commit

None — per repo rules (AGENTS.md / PROJECT.md), commits are disabled without explicit authorization.
