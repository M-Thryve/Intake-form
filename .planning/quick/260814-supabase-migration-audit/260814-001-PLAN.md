# Quick Task 260814-001: Supabase Migration Audit + Apply Prep

## Description

Review whether all tables/database are migrated to Supabase; prepare and execute the migration
apply plan against project `ilbyzsktnllevfbomesc`.

## Findings (review phase)

- The live `public` schema is effectively empty: 39/40 expected tables return 404
  (`PGRST205` — not in schema cache); only a legacy, unrelated `idempotency_keys`
  table exists (booking-API remnant: `id` / `route` / `key_hash`, 3 rows).
- Project is reachable and the service key is valid (`auth/v1/health` → 200, GoTrue v2.195.0).
- Repo confirms migrations were never applied to a live project (DEPLOYMENT.md manual apply
  instructions; PHASE_4 "Migration 003 not applied"; PHASE_9 "deployment-gate").
- CONFLICT: migration `000` uses `CREATE TABLE IF NOT EXISTS public.idempotency_keys` with a
  different schema than the legacy live table — must be renamed first or the create is skipped.

## Plan

1. Generate an ordered, section-marked bundle of all 16 migrations
   (`000_phase2_intake_schema.sql` → `015_phase2_atomic_submit.sql`). [DONE]
2. Generate pre-flight SQL to rename legacy `idempotency_keys` → `idempotency_keys_legacy`. [DONE]
3. User runs pre-flight + migrations in Supabase Dashboard SQL Editor in order. [PENDING — user]
4. Re-verify: probe all 40 expected tables against live project; confirm `idempotency_keys`
   schema matches the migration; report table-by-table diff. [PENDING]

## Artifacts

- `migrations-bundle.sql` — ordered, section-marked bundle (2652 lines)
- `preflight-idempotency-keys.sql` — conflict-resolution SQL
- This file (`260814-001-PLAN.md`)
